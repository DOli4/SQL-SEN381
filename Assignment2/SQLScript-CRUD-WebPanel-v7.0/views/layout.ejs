<!doctype html>
<html>
<head>
  <!-- ===================================================================
       Document head
       - Sets character encoding and responsive viewport
       - Defines the page title
       - Loads Bootstrap CSS from CDN for layout/components
       - Loads a small custom stylesheet from /public/style.css
       =================================================================== -->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>SQL Script Web Panel</title>

  <!-- Bootstrap 5 CSS (no JS required for current UI). In production, consider pinning integrity/crossorigin. -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- App-specific overrides (served by Express from /public). Keep this after Bootstrap so it can override defaults. -->
  <link href="/style.css" rel="stylesheet">
</head>
<body class="bg-body-tertiary">
  <!-- ===================================================================
       Top navigation bar
       - Minimal brand link that routes to the CRUD dashboard
       - Right-side container left empty for future items (e.g., diagnostics)
       =================================================================== -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container d-flex justify-content-between">
      <a class="navbar-brand" href="/crud">CRUD</a>
      <div class="d-flex gap-2">
        <!-- Placeholder for future nav items (e.g., /panel, /diag/sql) -->
      </div>
    </div>
  </nav>

  <!-- ===================================================================
       Main content
       - express-ejs-layouts injects each view’s rendered HTML into <%- body %>
       - Spacing provided by Bootstrap utility classes
       =================================================================== -->
  <main class="container py-4">
    <%- body %>
  </main>

  <!-- ===================================================================
       Notes:
       - No Bootstrap JS is required for the current pages. If components
         like modals/dropdowns are added later, include the Bootstrap bundle:
         <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
       - Consider security headers (e.g., helmet) and CSP in the Express app
         when deploying beyond local development.
       =================================================================== -->
</body>
</html>

