<!-- views/dashboard.ejs -->
<%- include('partials/navbar', { user }) %>

<style>
  body { background:#f5f6f8; font-family: Inter, system-ui, Arial; }

  .wrap { max-width: 1200px; margin: 24px auto; padding: 0 20px; }

  .page-title {
    font-size: 22px; font-weight: 800; text-align: center; margin: 8px 0 22px;
  }

  /* grid */
  .grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 18px;
  }
  @media (max-width: 1100px) { .grid { grid-template-columns: repeat(2, 1fr); } }
  @media (max-width: 720px)  { .grid { grid-template-columns: 1fr; } }

  /* card */
  .card {
    background:#fff; border:1px solid rgba(0,0,0,.06); border-radius: 16px;
    overflow:hidden; box-shadow: 0 10px 24px rgba(0,0,0,.06);
    display:flex; flex-direction:column;
  }
  .card-banner {
    width:100%; aspect-ratio: 16/9; background:#e5e7eb;
    object-fit: cover;
  }

  .card-body {
    padding: 14px 16px 18px;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .course-name { font-weight:700; font-size:15px; }

  /* row: button on left, tutor on right */
  .row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
}

  /* Enter button */
  /* make the Enter pill compact no matter what globals exist */
  .card .btn-enter{
    display: inline-flex !important;   /* beat any a{display:block} */
    flex: 0 0 auto;                    /* don't grow/shrink in flex row */
    width: auto !important;            /* never 100% */
    max-width: max-content;            /* hug text */
    line-height: 1.1;
    padding: 4px 10px;                 /* smaller pill */
    border-radius: 10px;
    border: 1px solid #7b0086;
    background: #fff;
    color: #7b0086;
    font-size: 12px;
    font-weight: 700;
    text-decoration: none;
    align-self: flex-start;            /* stick to the left of the row */
    margin-left: 6px;                  /* away from the card edge */
  }
  .card .btn-enter:hover{
    background:#7b0086; color:#fff;
  }

  /* keep the row layout */
  .card .row{
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:8px;
  }

  /* tutor info aligned right */
  .tutor {
    display:flex;
    align-items:center;
    gap:8px;
    justify-content:flex-end;
    text-align:right;
  }
  .tutor .avatar {
    width:30px; height:30px;
    border-radius:50%;
    background:#e5e7eb;
    display:grid; place-items:center;
    color:#6b7280;
    font-weight:700;
    flex-shrink:0;
  }
  .tutor .details {
    display:flex;
    flex-direction:column;
    line-height:1.2;
  }
  .muted { color:#6b7280; font-size:12px; }

  /* empty state */
  .empty {
    background:#fff; border:1px dashed #e5e7eb; border-radius:12px; padding:28px;
    text-align:center; color:#6b7280;
  }
</style>

<div class="wrap">
  <div class="page-title">My Courses</div>

  <% if (!courses || courses.length === 0) { %>
    <div class="empty">No courses yet.</div>
  <% } else { %>
    <div class="grid">
      <% courses.forEach(c => { %>
        <article class="card">
          <img class="card-banner" src="<%= c.banner || '/images/frontPage.jpg' %>" alt="">
          <div class="card-body">
            <div class="course-name"><%= c.name %></div>

            <div class="row">
              <a class="btn-enter" href="/courses/<%= c.id %>">Enter</a>
              
              <div class="tutor">
                <div class="details">
                  <div class="muted">Tutor</div>
                  <div><%= c.tutor || '—' %></div>
                </div>
                <div class="avatar">
                  <%= (c.tutor && c.tutor[0]) ? c.tutor[0].toUpperCase() : 'T' %>
                </div>
              </div>
            </div>
          </div>
        </article>
      <% }) %>
    </div>
  <% } %>
</div>
